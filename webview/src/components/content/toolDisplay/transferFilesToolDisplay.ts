import { IconTransfer } from '@tabler/icons-vue';
import type { ToolDisplayResolver } from './types';

export const transferFilesToolDisplay: ToolDisplayResolver = () => ({
  headerIcon: IconTransfer
});
